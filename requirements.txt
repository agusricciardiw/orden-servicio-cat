# Lo minimo para generar las ordenes (Word incluido)
openpyxl>=3.1
python-docx>=1.1

# Opcional: solo lo usa ver.py para rasterizar paginas a imagen
# pypdfium2>=4.0

# Opcional, solo Windows: exportar el PDF con Word.
# En Mac o Linux el PDF lo hace LibreOffice, que no se instala con pip.
# pywin32>=306 ; sys_platform == "win32"
